# Baked into the production build (vite build reads this automatically).
# The public URL of the Render backend — not a secret, safe to commit.
VITE_BACKEND_URL=https://groweasy-csv-backend-rjgn.onrender.com
